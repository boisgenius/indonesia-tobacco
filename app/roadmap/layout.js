export const metadata = {
  title: 'RYO Filter Roadmap | Indonesia Tobacco',
  description:
    'The execution graph for launching a retail RYO acetate filter line at PT ITSFR, from product sample and excise consultation through licensing to the first OEM export sale.',
};

export default function RoadmapLayout({ children }) {
  return children;
}
